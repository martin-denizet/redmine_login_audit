<%=
  l("mail_la_#{@login_audit.success? ? 'success' : 'failure'}_body_text",
    :user_name => @login_audit.login,
    :source => @login_audit.source.upcase,
    :instance => Setting.host_name.to_s,
    :ip_address => @login_audit.ip_address,
    :date => format_time(@login_audit.created_on))
%>